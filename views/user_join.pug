extends layout

block content
  h1= title

  form(method='POST' action='')
    div.form-group
      label(for='secret_code') Secret:
      input#secret_code.form-control(type='password' name='secret_code')
    button(type='submit') check